# macOS
.DS_Store

# Claude Code
.claude/

# SSH keys / secrets
id_ecdsa.pem
id_ecdsa
*.pem
sshkey.txt

# DB credentials
api/config/database.php
# api/.htaccess: SetEnv で本番 DB credentials を保持するため repo に含めない。
# テンプレートは api/.htaccess.example を参照（H-01 解消、2026-04-22）
api/.htaccess
api/.htaccess.*.local

# User uploads
uploads/

# Server backups (sakura)
_backup_*/

# Business documents
*.docx
*.xlsx
*.pdf
*.pptx

# Internal credentials / 社外秘
docs/internal/

# Node
node_modules/

# VitePress build cache (intermediate, not deployed)
docs/manual/.vitepress/dist/
docs/manual/.vitepress/cache/

# VitePress Basic Auth credentials (never commit — Sakura 本番のみに配置)
public/docs-internal/.htpasswd
public/docs-internal/.htaccess

# /public/ ルート Basic 認証 credentials (sandbox eat.posla.jp、2026-04-22
# 追加)。ハッシュ済み .htpasswd と .htaccess はサーバー上のみで管理、
# local repo には入れない。
public/.htpasswd
public/.htaccess
.htpasswd

# IDE
.vscode/
.idea/

# Logs
*.log

# Docker — ローカル開発用データ（リポジトリにコミットしない）
docker/db/data/
docker-compose.override.yml
