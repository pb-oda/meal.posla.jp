# macOS
.DS_Store

# Claude Code
.claude/

# SSH keys / secrets
id_ecdsa.pem
id_ecdsa
*.pem
sshkey.txt

# DB credentials
api/config/database.php

# User uploads
uploads/

# Server backups (sakura)
_backup_*/

# Business documents
*.docx
*.xlsx
*.pdf
*.pptx

# Internal credentials / 社外秘
docs/internal/

# Node
node_modules/

# VitePress build cache (intermediate, not deployed)
docs/manual/.vitepress/dist/
docs/manual/.vitepress/cache/

# VitePress Basic Auth credentials (never commit — Sakura 本番のみに配置)
public/docs-internal/.htpasswd
public/docs-internal/.htaccess

# IDE
.vscode/
.idea/

# Logs
*.log

# Docker — ローカル開発用データ（リポジトリにコミットしない）
docker/db/data/
docker-compose.override.yml
