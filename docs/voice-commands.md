# KDS 音声コマンド・ハンズフリー運用仕様

**対象画面**: `public/kds/index.html`, `public/kds/sold-out.html`
**主な対象ファイル**: `public/kds/js/voice-commander.js`, `public/kds/js/kds-renderer.js`, `api/kds/orders.php`
**最終更新**: 2026-04-29

---

## 1. 目的

KDSは厨房内で使う画面であり、調理中は衛生面の理由で端末に触れない時間が多い。
そのため、POSLAのKDS音声操作は「AIに任せる」ことではなく、次の順番で人が迷わず操作できる状態を作る。

1. よく使う操作はブラウザ内のファストパスで即時実行する
2. 取り消し可能な操作は10秒取り消しで戻せるようにする
3. 破壊的な操作や一括操作は確認ダイアログを挟む
4. 曖昧な言い回しだけ、端末ごとの「AI補助ON」時にGeminiへ送る
5. 音声認識は長時間稼働で劣化する前提で、自動リフレッシュする

この方針により、KDSはAI補助OFFでも通常運用できる。

---

## 2. 音声ON/OFFとAI補助ON/OFF

### 2.1 音声ON/OFF

KDSヘッダーに `音声ON / 音声OFF` ボタンを自動挿入する。

| 項目 | 仕様 |
|---|---|
| 操作場所 | KDSヘッダー |
| 保存先 | `localStorage: mt_kds_voice_active` |
| 対象 | KDS画面と品切れ管理画面で共有 |
| 挙動 | ON時はWeb Speech APIで常時リスニング |
| 復帰 | ページ遷移後も前回ONなら自動復帰 |

### 2.2 AI補助ON/OFF

KDSヘッダーの音声ボタン横に `AI補助ON / AI補助OFF` ボタンを表示する。

| 項目 | 仕様 |
|---|---|
| 操作場所 | KDSヘッダーの音声ボタン横 |
| 初期値 | OFF |
| 保存先 | `localStorage: mt_kds_voice_ai_fallback` |
| 対象 | 端末ごと。壁掛け端末・厨房端末ごとに別設定 |
| OFF時 | 未対応の曖昧な言い回しをGeminiへ送らず、エラー表示で止める |
| ON時 | ファストパスで処理できない発話だけGeminiへ送る |

音声でも切替できる。

| 発話例 | 動作 |
|---|---|
| `AI補助オン` | 音声AI補助をON |
| `AI解析オン` | 同上 |
| `AIフォールバックオン` | 同上 |
| `AI補助オフ` | 音声AI補助をOFF |
| `AI解析オフ` | 同上 |
| `AIフォールバックオフ` | 同上 |

注意: `AIシェフ` は別機能。AI補助OFFでも、利用者が明示的にAIシェフを開いた場合はAIシェフ側のGemini処理は使われる。

---

## 3. 音声処理の流れ

KDS音声は次の順番で処理する。

1. Web Speech APIが日本語音声を文字列化する
2. ノイズフィルタで短すぎる発話・長すぎる発話・低信頼度のfinalを除外する
3. ファストパスに一致するか確認する
4. ファストパス一致ならGeminiを使わず即時処理する
5. ファストパス不一致かつAI補助OFFなら止める
6. ファストパス不一致かつAI補助ONならGeminiへJSON解析を依頼する
7. Geminiの返答が低確信度の場合は確認ダイアログを出す

実装上の状態は次の3状態。

| 状態 | 意味 |
|---|---|
| `off` | 音声OFF |
| `standby` | 音声待機中。発話を受け付ける |
| `processing` | API処理中または解析中。二重実行を避けるため発話を無視 |

---

## 4. ファストパス一覧

ファストパスはGeminiを使わない。厨房の通常操作は原則ここで完結する。

### 4.1 品目単位ステータス変更

品目名と動作語で、該当品目だけを更新する。

| 発話例 | 動作 |
|---|---|
| `ハンバーグ開始` | ハンバーグを調理中へ |
| `チキン南蛮完成` | チキン南蛮を提供待ちへ |
| `トンカツ提供済み` | トンカツを提供済みへ |

動作語:

| ステータス | 認識語 |
|---|---|
| `preparing` | 調理、開始、準備、じゅんび |
| `ready` | 完成、できた、できました、あがり、上がり、アップ、レディ、ready |
| `served` | 提供、済み、すみ、サーブ、サーヴ |

品目名は完全一致だけでなく、`定食` / `セット` / `弁当` / `丼` / `御膳` / `ランチ` / `プレート` などのサフィックスを除いて照合する。

### 4.2 卓単位ステータス変更

卓番号と動作語で、該当卓の未完了注文を更新する。

| 発話例 | 動作 |
|---|---|
| `T01 調理開始` | T01の未完了注文を調理中へ |
| `T01 完成` | T01の未完了注文を提供待ちへ |
| `T01 提供済み` | T01の未完了注文を提供済みへ |

認識する卓番号表現:

- `T01`
- `t1`
- `テーブル1`
- `1番`

### 4.3 卓単位の一括操作

`全品` / `全部` / `まとめて` を含む場合は、卓単位の一括操作として扱う。

| 発話例 | 動作 |
|---|---|
| `T01 全品調理開始` | T01の未完了注文をまとめて調理中へ |
| `T01 全品完成` | T01の未完了注文をまとめて提供待ちへ |
| `T01 全品提供済み` | T01の未完了注文をまとめて提供済みへ |

一括操作は確認ダイアログを出す。
声で `実行` / `やめる` と答えられる。5秒無回答なら自動キャンセルする。

### 4.4 注文取消

| 発話例 | 動作 |
|---|---|
| `T01 キャンセル` | T01の未完了注文を取消 |
| `T01 取消` | 同上 |
| `T01 全品キャンセル` | T01の未完了注文をまとめて取消 |

取消は確認ダイアログを出す。
取消理由には `音声取消: <発話内容>` を付ける。

### 4.5 直前操作の10秒取り消し

KDSのステータス更新・品目更新・一括更新の後、10秒間だけ取り消しできる。

| 発話例 | 動作 |
|---|---|
| `戻して` | 直前操作を戻す |
| `戻す` | 同上 |
| `今のなし` | 同上 |
| `さっきのなし` | 同上 |
| `直前を戻す` | 同上 |

制約:

- 10秒を過ぎると戻せない
- 通信断やstale状態では戻し操作も止める
- 取消済み品目は戻し対象から除外する

### 4.6 配膳完了の取り消し

KDS上部に `直近の配膳完了` を表示する。
直近8件まで、提供済み品目を `提供待ち` に戻せる。

これは画面操作の導線であり、音声の `戻して` と同じ10秒取り消しとは別に表示される。

### 4.7 卓ごとの残り確認

| 発話例 | 動作 |
|---|---|
| `T01 残り` | T01の残り品目数と内訳を読み上げ |
| `T01 未完成` | 同上 |
| `T01 何が残ってる` | 同上 |

読み上げ内容:

- 残り合計
- 提供待ち数
- 調理中数
- 受付数
- 品目名、数量、状態

### 4.8 卓ごとの注意確認

| 発話例 | 動作 |
|---|---|
| `T01 注意確認` | T01の注意事項を読み上げ |
| `T01 アレルギー` | アレルギー・指定を読み上げ |
| `T01 指定` | メモ・オプション指定を読み上げ |

対象:

- 注文メモ
- アレルギー選択
- オプション指定

### 4.9 表示モード切替

| 発話例 | 動作 |
|---|---|
| `まとめ表示` | Expeditor向けのまとめ表示へ |
| `提供順` | 同上 |
| `エクスペディター` | 同上 |
| `通常表示` | 通常3列表示へ |
| `3列` | 同上 |

画面上の表示モードは `localStorage: kds_view_mode` に保存される。

### 4.10 コース操作

| 発話例 | 動作 |
|---|---|
| `T01 次フェーズ` | T01のコースを次フェーズへ |
| `T01 次のフェーズ` | 同上 |
| `T01 コース進めて` | 同上 |

確認ダイアログを出す。
対象卓にコース注文がない場合は実行しない。

### 4.11 ステーション切替

| 発話例 | 動作 |
|---|---|
| `全品目` | 全品目表示へ |
| `ドリンク` | ドリンクステーションへ |

ステーション名はKDS画面に表示されている `.kds-station-btn` の表示名を使って動的に認識する。

### 4.12 品切れ管理

| 発話例 | 動作 |
|---|---|
| `唐揚げ 品切れ` | 唐揚げを品切れにする |
| `唐揚げ 売り切れ` | 同上 |
| `唐揚げ 販売再開` | 唐揚げの品切れを解除 |
| `唐揚げ 解除` | 同上 |
| `品切れ一覧` | 現在の品切れ一覧を表示 |
| `品切れ確認` | 同上 |
| `品切れ管理` | 品切れ管理画面へ移動 |
| `売り切れ管理` | 同上 |

KDS画面ではメニューデータを `GET /api/kds/sold-out.php` から読み込み、品目名で照合する。
品切れ画面への移動時は音声認識インスタンスを停止してから `sold-out.html` に遷移する。

### 4.13 スクロール

| 発話例 | 動作 |
|---|---|
| `下にスクロール` | 画面を下へスクロール |
| `スクロールダウン` | 同上 |
| `上にスクロール` | 画面を上へスクロール |
| `スクロールアップ` | 同上 |

### 4.14 スタッフ呼び出し

| 発話例 | 動作 |
|---|---|
| `スタッフ呼んで` | ハンディ側へ呼び出し通知 |
| `ホールに来て` | 同上 |
| `呼び出し` | 同上 |

人物語:

- スタッフ
- すたっふ
- ホール
- ほーる

動作語:

- 呼んで
- 読んで
- よんで
- 来て
- きて
- お願い
- コール

### 4.15 テーマ切替

| 発話例 | 動作 |
|---|---|
| `ライトモード` | ライトテーマへ |
| `明るく` | 同上 |
| `ダークモード` | ダークテーマへ |
| `暗く` | 同上 |
| `テーマ変更` | ライトテーマへ |

### 4.16 AIシェフ

| 発話例 | 動作 |
|---|---|
| `シェフ` | AIシェフパネルを開く |
| `ダッシュボード` | 同上 |
| `シェフ閉じて` | AIシェフパネルを閉じる |
| `シェフ更新` | AIシェフデータを更新 |
| `最新にして` | 同上 |

AIシェフは明示操作で開く分析パネルであり、AI補助ON/OFFとは別管理。

### 4.17 音声再起動

| 発話例 | 動作 |
|---|---|
| `音声再起動` | 音声認識を完全再起動 |
| `音声リスタート` | 同上 |
| `音声リセット` | 同上 |
| `再起動して` | 同上 |
| `リスタート` | 同上 |

実行時は `SpeechRecognition` を停止・破棄し、AudioContextも作り直してから再開する。

### 4.18 コマンド一覧

| 発話例 | 動作 |
|---|---|
| `コマンド一覧` | KDS画面内に音声コマンド一覧を表示 |
| `コマンドリスト` | 同上 |
| `ヘルプ` | 同上 |
| `使い方` | 同上 |
| `何ができる` | 同上 |

15秒後に自動で閉じる。タップでも閉じられる。

---

## 5. Gemini解析

Gemini解析は、ファストパスで処理できない発話だけが対象。
さらに、端末の `AI補助` がONの場合だけ実行する。

### 5.1 AI補助OFF時

未対応の発話はGeminiへ送らない。

表示:

```text
AI補助OFF: 未対応の音声コマンドです。「コマンド一覧」で確認してください
```

### 5.2 AI補助ON時

`POST ../../api/store/ai-generate.php` に、次の情報を含めて解析を依頼する。

- 発話文字列
- 現在の注文一覧
- メニュー品目一覧
- 操作候補

GeminiにはJSONのみを返すように指示する。

対応する解析種別:

- 注文単位ステータス変更
- 品目単位ステータス変更
- 品切れ設定
- 品切れ解除
- 品切れ一覧
- 品切れ管理画面へ移動

### 5.3 低確信度時の確認

Geminiが `confidence: "low"` を返した場合は確認ダイアログを出す。
声で `実行` または `やめる` と答えられる。
5秒無回答なら自動キャンセルする。

---

## 6. KDS表示機能

### 6.1 通常3列表示

通常表示は次の3列。

- 受付
- 調理中
- 提供待ち

品目単位でステータスを分けて表示する。
同じ注文内に受付品目と提供待ち品目が混在する場合、該当品目だけが各列に表示される。

### 6.2 Expeditorまとめ表示

`まとめ (提供順)` はExpeditor向けの1カラム表示。

表示内容:

- 提供待ち合計
- 未完成品合計
- 卓ごとの残り数
- 卓ごとの提供待ち・調理中・受付数
- 注文カード内の `提供可能` / `調理中` / `受付` セクション

並び順:

1. 提供待ち品目を持つ注文
2. 赤・黄の遅延優先度
3. 経過時間が長い注文

混在状態の注文では、誤操作防止のため一括ボタンを抑止し、品目ごとの操作を促す。

### 6.3 遅延優先表示

KDSは注文ごとに優先度を自動判定する。

| 条件 | 表示レベル |
|---|---|
| 経過10分以上 | warning |
| 経過20分以上 | danger |
| テイクアウト受取時刻まで10分以内 | warning |
| テイクアウト受取時刻を超過 | danger |
| 予約時刻到達 | warning |
| 予約時刻から10分以上超過 | danger |

表示理由はカード上に表示される。

例:

- `経過12:30`
- `受取18:30まで08:00`
- `受取18:30超過`
- `予約19:00`
- `予約19:00超過`

予約時刻は `api/kds/orders.php` が、現在の卓セッションに紐づく予約から `reservation_reserved_at` として返す。
予約テーブルが未適用の環境では無視する。

### 6.4 メモ・アレルギー・指定の強調

KDSカードでは次を大きく表示する。

| 種別 | 判定元 |
|---|---|
| 注文メモ | `orders.memo` |
| アレルギー | `order_items.allergen_selections` |
| 辛さ指定 | メモ・オプションに `辛`, `激辛`, `ピリ辛`, `唐辛子`, `チリ`, `spicy` |
| 抜き/なし指定 | メモ・オプションに `抜き`, `なし`, `無し`, `除く`, `不使用`, `入れない` |
| 温度/温め指定 | メモ・オプションに `温め`, `あたため`, `熱め`, `ぬるめ`, `冷たい`, `冷やし` |
| 焼き加減/仕上げ指定 | メモ・オプションに `よく焼き`, `ウェルダン`, `レア`, `固め`, `柔らか`, `別添え`, `少なめ`, `多め` |

アレルギー表示ラベル:

| キー | 表示 |
|---|---|
| `egg` | 卵 |
| `milk` | 乳 |
| `wheat` | 小麦 |
| `shrimp` | えび |
| `crab` | かに |
| `buckwheat` | そば |
| `peanut` | 落花生 |

---

## 7. 音声認識の安定化

Web Speech APIはブラウザ依存であり、Chromeでも長時間稼働すると認識品質が落ちることがある。
POSLA KDSではこの前提で、自動メンテナンスを行う。

### 7.1 自動リフレッシュ

音声ON中、20秒ごとに状態を監視する。
ただし、20秒ごとに再起動するわけではない。
条件に当たった時だけハードリセットする。

| 条件 | 動作 |
|---|---|
| 12分経過 | 予防リフレッシュ |
| 90秒応答なし | ハードリセット |
| 3分final未確定かつinterim/エラーあり | ハードリセット |
| interimだけ45秒継続 | ハードリセット |
| 3分内にno-speech 4回 | ハードリセット |
| 3分内にnetwork 2回 | ハードリセット |
| 3分内にaudio-capture 2回 | ハードリセット |

ハードリセット手順:

1. interim確定タイマーを止める
2. `SpeechRecognition.abort()` を呼ぶ
3. `recognition = null` にする
4. 350ms待つ
5. `SpeechRecognition` を新規作成する
6. 待機状態へ戻す

### 7.2 自動リフレッシュしない場面

誤操作防止のため、次の場面では自動リフレッシュしない。

- API処理中
- Gemini解析中
- 確認ダイアログ表示中
- 手動停止中
- すでにハードリセット中

### 7.3 手動再起動

自動リフレッシュで復帰しない場合に備え、手動の `音声再起動` は残す。
厨房では端末に触れない前提のため、通常は自動リフレッシュを優先する。

---

## 8. 通信断・stale時の扱い

KDSが通信断またはstale状態の場合、状態変更系の操作は止める。

対象:

- 調理開始
- 完成
- 提供済み
- 取消
- 10秒取り消し
- 品切れ更新
- 一括操作
- コース次フェーズ
- スタッフ呼び出し

画面上のボタンはdisabled化され、音声操作も成功音を鳴らさない。
通信復帰後、次のポーリングで操作可能に戻る。

---

## 9. 推奨端末運用

| 項目 | 推奨 |
|---|---|
| ブラウザ | Chrome |
| 音声AI補助 | 通常OFF。ファストパスで足りない店だけON |
| マイク | 可能ならUSB/有線マイク。Bluetoothは省電力・接続切れに注意 |
| 画面 | スリープしない設定 |
| 設置 | 排気音・食洗機・BGMスピーカーの近くを避ける |
| 確認 | 開店前に `音声ON`, `コマンド一覧`, `T01 残り` などで反応確認 |

Web Speech APIの日本語モードは `T01` のような英数字を常に正しく認識するとは限らない。
運用では `テーブル1`, `1番` も併用できるようにしている。

---

## 10. 現時点の未実装・今後検討

現物確認時点で、次は未実装または運用検証が必要。

| 項目 | 理由 |
|---|---|
| 実機での30分以上の音声耐久テスト | Web Speech APIは端末・マイク・厨房環境依存が大きい |
| 音声成功率ログ | どのコマンドが失敗しているかを端末単位で把握するため |
| 端末別マイク診断画面 | 入力レベル・権限・最後のfinal時刻・自動リフレッシュ回数を現場で見たい |
| ステーション別の通知音・強調 | 揚げ場/ドリンク/焼き場で見るべき注文が違うため |
| 予約/テイクアウトの事前アラート | 受取時刻直前ではなく、仕込み開始の目安を出す必要がある店舗向け |
| コース提供の詳細制御 | フェーズ単位の開始/停止/待機をより明確にする余地がある |
