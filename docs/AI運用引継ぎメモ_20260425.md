# AI運用引継ぎメモ — 2026-04-25

このメモは、`【擬似本番環境】meal.posla.jp` を次セッションで **AI運用確認の接続先** として使うための引き継ぎです。

## 現在の前提

- 対象環境: `【擬似本番環境】meal.posla.jp`
- ローカル配信 URL: `http://127.0.0.1:8081`
- 起動方式: このフォルダで `docker compose up -d`
- 役割: 本番直前の vendor-neutral 検証、dry-run、AI運用支援の接続先

## 現在の状態

- `2026-04-25` 時点で擬似本番の主要スモークは `Failures: 0`
- `api/monitor/ping.php` は `ok:true`
- 顧客側、POSレジ、POSLA管理画面、監視、docs 配信は現時点で大きな blocker なし
- POSLA管理画面の運用強化機能は反映済み
  - テナント健全性スコア
  - オンボーディング完了率
  - リリース準備状況
  - 危険テナント上位表示
  - 設定差分・変更履歴
  - テナント別障害タイムライン
  - 運用タイムライン
  - 秘密値充足チェック
  - 通知テストセンター
  - 障害調査ビュー
- 2026-04-26 時点で `test-01` cell は 1 tenant / 1 cell として稼働中
  - control snapshot で `onboarding_progress=100`
  - control snapshot で `health_score=100`
  - control snapshot で `cell_tier0_status=ok`
  - `owner / manager / staff / device` の初期ロールは `ensure-ops-users` で補完済み

## 直近で潰した見えにくい不具合

- `api/store/receipt-settings.php`
  - GET 権限を `require_auth()` から `require_role('staff')` に修正
  - `device` で領収書/インボイス設定を読める状態を解消
- `public/kds/js/cashier-app.js`
  - `会計取消` ボタンの表示を `manager / owner` のみに修正
  - backend の `api/store/payment-void.php` と role 条件を一致させた

## AI運用セッションへ切り替えるときの注意

- **Docker は落とさない**
  - `127.0.0.1:8081` が生きている前提で AI運用確認を進める
- `docker compose down -v` は実行しない
  - DB volume が消える
- ローカルの bind mount が正本
  - `api/`, `public/`, `docs/`, `scripts/` の実ファイルがそのまま配信される

## 次セッションで最初に確認すること

1. `pwd` で `【擬似本番環境】meal.posla.jp` にいることを確認
2. `docker compose ps` で `php` / `db` が起動中か確認
3. `curl -sf http://127.0.0.1:8081/api/monitor/ping.php` で `ok:true` を確認
4. AI運用側の接続先を `http://127.0.0.1:8081` に向ける
5. POSLA管理画面ダッシュボードの「リリース準備状況」を確認する
6. cell を追加した場合は `scripts/cell/cell.sh <cell-id> smoke` と `POSLA_CELL_SMOKE_STRICT=1 scripts/cell/cell.sh <cell-id> smoke` を実行する

## 補足

- docs 再ビルド用に `docs/manual/node_modules/` は残してある
- 本番ドキュメントは `<production-domain>` などのプレースホルダで維持する
- `eat.posla.jp` はデモ環境の参考情報としてのみ扱う
